# Node Express CRUD Example With MongoDB

test

mongod --dppath==/root/data/db --port 27017 --fork --logpath /root/data/db/mongo.log

## How To Install and RUN
On terminal
- npm install
- sudo npm install -g nodemon
- nodemon
- http://localhost:3000/